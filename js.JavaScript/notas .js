java es un nuevo idioma, empesamos a utilizar el programa codepen para 
aplicar codigo code pen no
lo descargamos solo por internet me rejistre por medio de facebook;


var es una funcion que significa variable abreviado pero no es variable 
es una funcion lo que es variable es lo que est a lado mde esta funcion 

let casi funcina igual que var pero no se puede utiliza dos veses casi es 
como un id# se utiliza solo una ves al querer utilizar 2 veses te parese
un error te dice que la lo utilizaste una vez.a

(este es un selector de clase si al querySelector le agregas all como lo 
    tenemos aqui quiere decir que buscamos todos que le se llamen asi todos.
    y si no tiene el all quiere decir que presizamente esa clase que se esta 
    poniendo en el parentesis.
    
    const images = document.querySelectorAll('.FFVAD')
    )